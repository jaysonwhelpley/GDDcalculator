<%= form_with(model: sentinel, local: true) do |form| %>
  <% if sentinel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sentinel.errors.count, "error") %> prohibited this sentinel from being saved:</h2>

      <ul>
      <% sentinel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
